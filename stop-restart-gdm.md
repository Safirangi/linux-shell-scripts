# Stopping and Restarting Display manager (ubuntu - gdm)

## to stop gdm

`sudo systemctl stop gdm`

to restart the gdm, `ALT + F2`, then login in the terminal console and run the following command

`sudo systemctl start gdm`
